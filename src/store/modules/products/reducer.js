import initialState from "../../../js/products";

const reducerProducts = (state = initialState, action) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default reducerProducts;
